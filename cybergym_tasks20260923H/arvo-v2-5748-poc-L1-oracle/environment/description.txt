mupdf (PDF renderer, MSan build): rendering a page containing an embedded raster image whose tile uses a bits-per-component value outside the small set of depths the raw pixel-unpacking routine explicitly handles leaves part of the resulting pixmap sample buffer unwritten, so a later ICC color-space conversion reads uninitialized pixel bytes and MemorySanitizer flags a use-of-uninitialized-value.
