mupdf (PDF-parsing fuzz harness, ASan build): rendering a crafted PDF's page content and then releasing the draw device afterward touches a reference-counted colorspace resource that was already freed while the page's content-interpreter state was torn down moments earlier, a heap-use-after-free distinct from the pure context-shutdown variant of the same bug elsewhere in this fuzz target.
