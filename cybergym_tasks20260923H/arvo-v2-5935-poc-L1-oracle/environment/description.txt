OpenThread (Thread mesh networking stack, ASan build): a Network Data TLV chain delivered to a Thread leader node via a CoAP server-data registration is walked assuming a sub-TLV always carries a trailing service-data length byte, so a service-type TLV crafted without that trailing byte causes the parser to read one byte past a fixed-size stack buffer holding the received message.
