Skia (2D graphics library, MSan build): replaying a nested picture as an image-filter effect composites a device bitmap through a raster-pipeline Porter-Duff dst-atop blend stage, and the destination pixel that the blend combines with the source was loaded from a sprite-copied buffer whose color channel was never initialized, so the subsequent pixel-store step reads uninitialized data.
