ImageMagick (MSan build): reading a MIFF image's embedded metadata profile block accepts the block even when the blob stream hit end-of-file before the announced profile length was fully read, leaving the unread tail of the profile buffer uninitialized; scanning that profile as a photoshop-style resource block later compares the uninitialized tail bytes against known resource-name prefixes.
