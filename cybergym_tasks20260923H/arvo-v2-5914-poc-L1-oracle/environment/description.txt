Wireshark (NBAP - UMTS Node B Application Part dissector, generated-code ASan build): tracking per-channel state while walking transport-format-set information elements uses a channel identifier parsed straight from the message, including an out-of-band "unset" sentinel value, as an unchecked index into a fixed-size per-call channel-state table, producing a wild out-of-bounds read/SEGV when a crafted PDU supplies that sentinel.
