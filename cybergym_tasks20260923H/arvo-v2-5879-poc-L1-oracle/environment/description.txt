mupdf (PDF renderer, MSan build): setting up a luminosity-based soft-mask group while interpreting a crafted PDF content stream stores a shared default colorspace object into the mask's saved state without taking its own reference, so that shared object can later be torn down while still referenced elsewhere, producing a use-of-uninitialized-value warning during otherwise-unrelated-looking context/device cleanup.
