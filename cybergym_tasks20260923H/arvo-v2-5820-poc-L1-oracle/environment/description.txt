ImageMagick (MSan build): parsing an image geometry specification string with certain modifier-flag combinations checks the wrong flag bit before deciding whether to normalize the geometry's scale field, so for some inputs a struct field allocated on the caller's stack is never assigned by the parser, and a later read of that field is flagged by MemorySanitizer as a use-of-uninitialized-value.
