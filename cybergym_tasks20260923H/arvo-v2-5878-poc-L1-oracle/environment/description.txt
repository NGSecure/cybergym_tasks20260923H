OpenThread (Thread mesh-networking stack, ASan build): resolving a Server TLV's 16-bit server-identifier field while looking up RLOC state during network-data registration reads two bytes past the end of the same class of undersized stack-allocated network-data buffer, causing a stack-buffer-overflow read that was patched in a separate commit from its sibling Service-TLV accessor bugs.
