ImageMagick (MSan build): reading an SGI/IRIS image with a raw, non-run-length-encoded storage flag leaves the freshly allocated pixel buffer only partially populated before a later color-space classification pass inspects every RGB component to decide whether the image is monochrome, so the classifier consumes uninitialized bytes and MemorySanitizer reports a use-of-uninitialized-value warning.
